"use client";

// All active layouts are self-contained — projects are rendered within each
// Hero component. This file is kept as a mount point but renders nothing.
export default function Projects() {
  return null;
}
